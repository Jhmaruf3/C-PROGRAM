/*
#include <stdio.h>
#include <stdlib.h>

int main()
{
    double x=12;
    double result=log(x);

    printf("log(%lf)=%.2lf",x,result);
}
*/
#include <stdio.h>
#include <stdlib.h>

int main()
{
    double x=3.34;
    double result=round(x);

    printf("round%lf)=%.2lf",x,result);
}
